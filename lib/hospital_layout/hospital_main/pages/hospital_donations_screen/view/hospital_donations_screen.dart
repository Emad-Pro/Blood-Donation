import 'package:flutter/material.dart';

class HospitalDonationsScreen extends StatelessWidget {
  const HospitalDonationsScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Column();
  }
}
